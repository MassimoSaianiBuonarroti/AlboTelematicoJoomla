<?xml version="1.0" encoding="utf-8"?>
<form>
    <fields name="filter">
        <!-- Filtro testo (cerca su titolo e numero documento) -->
        <field
            name="search"
            type="text"
            label="Cerca"
            description="Cerca per titolo o numero documento"
        />

        <!-- Filtro categoria (usa la stessa tabella delle categorie) -->
        <field
            name="category"
            type="sql"
            label="Categoria"
            query="SELECT id AS value, title AS text FROM #__albo_categorie WHERE state = 1 ORDER BY ordering, title"
            key_field="value"
            value_field="text"
            required="false"
            multiple="false"
        />
    </fields>
</form>
